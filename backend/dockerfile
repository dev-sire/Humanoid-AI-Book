# backend/Dockerfile

# Use an official Python image as the base
FROM python:3.11-slim

# Set the working directory inside the container
WORKDIR /app

# Copy requirements.txt and install dependencies
# We copy this first for optimal Docker layer caching
COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

# Copy the rest of the backend application code
# This copies everything from backend/ to /app in the container
COPY . .

# Expose the port Uvicorn will run on
EXPOSE $PORT

# Define the command to run your application using Uvicorn
# $PORT is automatically injected by Railway
CMD ["uvicorn", "main:app", "--host", "0.0.0.0", "--port", "8080"]
# Note: Use 8080 or any other static port here. Railway maps this to $PORT.